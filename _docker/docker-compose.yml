---
version: '3.4'
services:
  weaviate:
    command:
    - --host
    - 0.0.0.0
    - --port
    - '8080'
    - --scheme
    - http
    image: cr.weaviate.io/semitechnologies/weaviate:1.25.7
    ports:
    - 8080:8080
    - 50051:50051
    volumes:
    - weaviate_data:/var/lib/weaviate
    restart: on-failure:0
    environment:
      QUERY_DEFAULTS_LIMIT: 25
      AUTHENTICATION_ANONYMOUS_ACCESS_ENABLED: 'true'
      PERSISTENCE_DATA_PATH: '/var/lib/weaviate'
      DEFAULT_VECTORIZER_MODULE: 'none'
      ENABLE_MODULES: 'text2vec-ollama,generative-ollama,text2vec-aws,generative-aws,text2vec-cohere,generative-cohere,text2vec-openai,generative-openai,text2vec-palm,generative-palm,multi2vec-palm,text2vec-huggingface,text2vec-jinaai,generative-anyscale'
      CLUSTER_HOSTNAME: 'node1'
volumes:
  weaviate_data:
...